# 05 — Modules

Creating reusable modules and composing infrastructure from module calls.

## What This Covers

- Module structure: `variables.tf`, `main.tf`, `outputs.tf`
- Local modules vs registry modules
- Module inputs and outputs
- `for_each` with modules
- The `modules/` directory at the repo root for shared modules

## Status

> Planned — coming after 04-data-sources is complete.
