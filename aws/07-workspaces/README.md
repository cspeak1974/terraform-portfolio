# 07 — Workspaces

Using Terraform workspaces for environment separation.

## What This Covers

- `terraform workspace` commands: `new`, `select`, `list`, `show`
- `terraform.workspace` interpolation
- When to use workspaces vs separate state files
- Workspace-aware variable patterns

## Status

> Planned — coming after 06-state-management is complete.
