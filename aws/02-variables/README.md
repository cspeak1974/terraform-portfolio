# 02 — Variables

Input variables: types, defaults, validation, and sensitive values.

## What This Covers

- Variable types: `string`, `number`, `bool`, `list`, `map`, `object`, `tuple`
- Default values and required variables
- Variable validation blocks
- Sensitive variables and masking
- Precedence: CLI flags, `.tfvars`, environment variables, defaults

## Status

> Planned — coming after 01-basics is complete.
