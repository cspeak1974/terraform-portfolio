# Shared Modules

Reusable Terraform modules shared across provider examples.

## Status

> Placeholder — modules will be extracted here as patterns emerge across the AWS and Azure tracks.

## Conventions

Each module lives in its own subdirectory and follows the standard structure:

```
modules/<module-name>/
├── main.tf
├── variables.tf
├── outputs.tf
└── README.md
```
